<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/main.css">
    <title>Register | Lego Collection</title>
</head>
<body class="min-h-screen bg-black">
    <%- include('partials/navbar', { page: '/register' }) %> <!-- Navbar partial -->
    
    <div class="container mx-auto px-4 py-6 md:p-8">
        <div class="border-4 border-red-600 bg-zinc-900 p-4 md:p-8 mb-6 md:mb-8">
            <div class="text-center max-w-3xl mx-auto">
                <% if (!successMessage) { %>
                <h1 class="text-4xl md:text-5xl font-mono font-black text-white mb-4">Register</h1>
                <form action="/register" method="POST" class="space-y-4">
                    <input 
                        type="text" 
                        name="userName" 
                        placeholder="User Name" 
                        required 
                        value="<%= userName || '' %>"
                        class="w-full px-4 py-2 text-white bg-zinc-800 border-2 border-red-600 rounded focus:ring-4 focus:ring-red-600 outline-none"
                    >
                    <input 
                        type="password" 
                        name="password" 
                        placeholder="Password" 
                        required 
                        class="w-full px-4 py-2 text-white bg-zinc-800 border-2 border-red-600 rounded focus:ring-4 focus:ring-red-600 outline-none"
                    >
                    <input 
                        type="password" 
                        name="password2" 
                        placeholder="Confirm Password" 
                        required 
                        class="w-full px-4 py-2 text-white bg-zinc-800 border-2 border-red-600 rounded focus:ring-4 focus:ring-red-600 outline-none"
                    >
                    <input 
                        type="email" 
                        name="email" 
                        placeholder="Email Address" 
                        required 
                        class="w-full px-4 py-2 text-white bg-zinc-800 border-2 border-red-600 rounded focus:ring-4 focus:ring-red-600 outline-none"
                    >
                    <button 
                        type="submit" 
                        class="w-full px-6 py-2 bg-red-600 text-white font-bold rounded hover:bg-red-700 transition-colors"
                    >
                        Register
                    </button>
                </form>
                
                <% if (errorMessage) { %>
                <div class="mt-4 text-red-500 bg-zinc-800 border border-red-600 p-4 rounded flex items-center">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 shrink-0 mr-2" fill="none" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <span><%= errorMessage %></span>
                </div>
                <% } %>
                <% } else { %>
                <div class="text-center text-green-400 bg-zinc-800 border border-green-600 p-4 rounded">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 inline-block mb-2" fill="none" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <p><%= successMessage %></p>
                    <a href="/login" 
                       class="mt-4 inline-block px-6 py-2 bg-green-600 text-white font-bold rounded hover:bg-green-700 transition-colors">
                        Proceed to Log in
                    </a>
                </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
